---
status: complete
updated: 2026-08-24T17:03
---

- (decision) The eligibility rule is enforced in the bundle command, not asked for in prose: a feature that is not spec-approved cannot be bundled, and the error says why — the bundle is a contract with development. A status claiming spec-approved with no spec.md on disk is caught separately.
- (decision) The pre-flight blocker report warns and never refuses, deliberately differing from the spec-approval gate which does refuse. A BA shipping with a known open question is making a legitimate call; the number is recorded on the phase either way, and the report shows it against previous phases so a rising trend is visible.
- (decision) The bundle hands bmad-prd an ordered list of PATHS, never a flattened document. bmad-prd subagent-extracts source documents, so pre-merging destroys the structure it reads and blows out context for nothing. BUNDLE.md goes first as orientation for a reader with zero context.
- (decision) SKILL.md pins the closing order — feature-set to handed-off, then as-built, then phase-close, then phase-open — because each step reads what the previous one wrote. as-built only sees handed-off features, and phase-close refuses until they are.
- (note) The next-call agenda deliberately covers the whole phase rather than just the bundled features. The client still owes those answers even for work that did not ship this round, and the agenda is what the next transcript closes questions against.
- (event) Requirement 4 demonstrated on the real store. Pre-flight found 1 eligible feature with 10 approved requirements and 0 critical blockers travelling into the PRD — because the spec-approval gate had already closed them. Bundle emitted BUNDLE.md, bundle.json and the next-call agenda, listing six source document PATHS for bmad-prd rather than a flattened document. as-built recorded Academy with all ten requirement ids, and phase-2 closed at 0 blockers against phase-1's 2.
- (event) The eval caught a real bug that would have fired on every genuine cross-phase handoff: the loader only read the target phase, so F-001's dependencies — both handed off in phase-1 — were reported as missing. Dependency readiness is now looked up across the whole registry, and the three cases are distinguished: in the bundle, shipped elsewhere, still in flight elsewhere (named with its phase), or not in the registry at all.
- (note) The blocker trend across the two phases came out 2 then 0. That is the number Vadim named as his own criterion for whether this method beats his manual one, and it is now recorded per phase rather than reconstructed.
- (defect by user) A phase PRD was generated for the live engagement carrying eleven open questions against a standard of none. Root cause: fdw-elaborate wrote questions as prose into spec.md and nothing minted them into feature.json, the ledger every gate reads — and nothing could, because no CLI verb existed that could add a question. Eight of the eleven had existed upstream all along, invisible. Pre-flight replayed at spec-approved reported '0 critical blockers would travel into the PRD' and phase-close recorded blocker_count_at_handoff: 0, a false zero on the module's own evaluation metric.
- (decision by user) Reversed the locked warns-never-refuses decision: bundle now refuses while a critical question is open. Gate is on bundle, not preflight — preflight is the report the BA runs to decide and die() returns before its --out HTML is written, so refusing there silently drops the artifact. Bundle is also the only place that catches a critical question raised AFTER approval, since the spec gate never runs again. Refusal names the clean features first and the override second; accepted ids go into bundle.json and BUNDLE.md. blocker_count_at_handoff deliberately NOT redefined — earlier phases recorded their score under the critical-only definition and redefining destroys the only cross-phase trend; open_question_count_at_handoff sits beside it.
- 2026-08-25 (decision) bundle now refuses on an open in-flight change (a delivered one never blocks a phase — it ships beside it). build-brief writes a bmad-build INTENT file, deliberately without status: frontmatter, because bmad-build routes a file that has it as a resumable spec; emitting its spec template would mean filling a Code Map this module cannot. The brief ranks delivered requirements by relevance to the change and reports what it omitted — testing on the real epp store, F-001's 33 requirements produced a 1583-token brief that nearly blew bmad-build's whole budget before the change was described; ranked to 8 it is 652. as-built gained --rebuild, preserves the original ship date on a rebuild, and the closing order moved to phase-close BEFORE as-built so prd_path is no longer always absent.
