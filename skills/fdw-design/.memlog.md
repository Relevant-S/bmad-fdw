---
status: complete
updated: 2026-08-22T15:56
---

- (decision) Relocated fdw_state.py from skills/fdw-intake/scripts/ to {project-root}/_bmad/fdw/scripts/ before starting this skill. fdw-design is the first skill besides intake that must write to the store, and a cross-skill script path would have been wrong in three more places by skill six. Mirrors how BMad ships memlog.py at _bmad/scripts/. Un-ignored _bmad/fdw in git so the module payload is version controlled.
- (decision) Added feature-set to the shared CLI with lifecycle gates enforced in code: a forward move cannot skip a stage, backward moves are always allowed because rework is normal, and --force logs an override to decisions.md. This is how 'only design-approved may enter speccing' stops being prose.
- (decision) ux-notes.md is a contract, not scratch. fdw-elaborate reads it as its primary input, so assumptions are numbered A1..An and tied to screens S1..Sn, and gaps from the empty-state pass are G1..Gn. The spec cites those ids, which is what makes a requirement traceable back to the screen that produced it.
- (direction) The empty-state walkthrough is a required pass with its own artifact, not an optional prompt. In the source transcript it was the move that made the model find its own gap; leaving it to chance would drop the highest-value technique in the method.
- (event) Eval on the real store passed across three skills. Inventory detected next+tailwind+pnpm and five components; scaffold created the two contract artifacts; the gate refused an untouched design with two named problems; a completed design (3 assumptions, 2 corrections, 3 empty-state gaps) passed; feature-set then refused sliced → client-review as a skipped gate and accepted the correct two-step path; fdw-status picked the change up immediately.
- (decision) Fixed a defect the eval exposed: check suggested a single feature-set command that the state CLI would then refuse, because the feature had not passed through designing. check now emits the real command sequence from wherever the feature actually is.
- (decision) Fixed a second self-inflicted defect: the scaffold templates shipped a placeholder A1 assumption and a dated correction line, so untouched boilerplate satisfied two of the four gate checks. Templates now show the shape inside HTML comments and ship no live entries; a test asserts an untouched scaffold cannot pass.
- (decision) Carved reproduce-as-is and the Figma bridge to references/brownfield.md. Both are conditional branches most runs never take, so every invocation would otherwise pay for them. SKILL.md 1550 tokens, reference 773.
