---
status: complete
updated: 2026-08-22T20:58
---

- (decision) Building fdw-intake as skill 1 of 9 in the fdw module; it defines the state-store data contracts the other eight consume. Source of truth: skills/reports/module-plan-ba-feature-discovery.md (status complete).
- (decision) Shape confirmed as ONE skill, not split into normalize + extract: the stages always run together on a single input and splitting would add a handoff with no user benefit.
- (decision) Contradiction detection scoped narrowly to incoming-source-vs-existing-state. Full cross-feature audit stays in fdw-consistency, which intake invokes. Prevents the two skills overlapping.
- (decision) State store owned by one script (scripts/fdw_state.py) that both fdw-intake and the future CM-generated fdw-setup call, so store creation is idempotent and has a single implementation.
- (direction) Adopt plan-validate-execute: the model emits an intake plan as JSON, the script validates it against the store with errors that name the fix, then applies it atomically. Batch writes across registry and feature folders must never half-apply.
- (decision) Machine state is JSON (registry.json, feature.json, sources/index.json), stdlib only. No PyYAML in the runtime, and YAML implicit typing would coerce ids/statuses/dates in the one file where that is most dangerous. All prose files stay markdown. Deviates from the plan doc's .yaml naming; plan doc to be amended.
- (decision) Accepted three additions beyond the plan: batch ingest of several sources in one run, supersede flow for near-identical re-exported sources (hash alone only catches byte-identical), and a read-only validate intent for registry-vs-folders integrity.
- (decision) Normalization is STRUCTURAL only, never translation. The source file keeps the original language verbatim because a provenance quote that has been translated is not evidence. English happens at extraction into signal.md, where each line carries English text plus the original-language quote.
- (decision) The provenance non-negotiable is enforced by validate-plan in code, not by prose in SKILL.md: a signal entry missing text, anchor, or quote fails validation and the plan cannot apply.
- (decision) No customize.toml. The module's real knobs (discovery_folder, prototype paths, client language) live in BMad config and are collected by fdw-setup; a second surface here would be redundant. Skill ships fixed.
- (event) Eval beat passed on real input. Ingested docs/ba-skills-discussion.json end to end: 118 transcript rows to 31 speaker turns, 3 features sliced (Academy, Events agenda layout, Session list improvements), 5 open questions raised, glossary seeded with Ukrainian ASR variants of 'spec' and 'PRD'. A simulated follow-up call then merged evidence into F-001 and auto-closed 2 questions against the speaker's verbatim words, dropping open blockers 5 to 3. Store validated healthy after every step.
- (event) 31 unit tests pass, covering provenance rejection, the sandbox rule (a contradiction against a spec-approved feature cannot apply without route=change-record, and spec.md is byte-identical afterwards), id non-reuse, near-match re-export detection, and stale-count drift.
- (decision) Amended the module plan doc: machine state is JSON, and skills/fdw-intake/assets/state-contract.md is now the canonical contract, copied into each store as CONTRACT.md at init so the store is self-describing.
- (decision) Analyze pass run over the skill (lenses applied sequentially in-context rather than as parallel subagents). Four defects found and fixed in the same session: missing Overview heading; the intake plan had no on-disk home so a compaction lost the run; the model was hand-transcribing a sha256 into its plan; and no fallback when subagents are unavailable. One low finding cut as padding. Grade: excellent, no criticals.
- (decision) Determinism fix generalized: normalize now records the source block to sources/.pending/{source_id}.source.json and validate-plan, apply-plan and record-empty all resolve from it. A plan carries only source_id. A hand-supplied hash that disagrees is rejected rather than silently written.
- (note) Residual advisory findings left open deliberately: on_complete hook for the fdw-consistency handoff (revisit once that skill exists), the path scanner's false positive on .memlog.md, and the provenance rule appearing in both the Overview bar and the Rules bullet (kept — the canon treats a stripped why as under-writing).
- (note) Contract blast radius is the real risk carried forward: assets/state-contract.md is load-bearing for eight unbuilt skills. Build fdw-status next as planned — it consumes the contract without writing to it, so the schema gets exercised while changing it is still cheap.
- (decision) The shared state CLI came back into this skill at scripts/fdw_state.py, alongside the contract asset it copies. It ships wherever fdw-intake ships, which is the property _bmad/fdw/ did not have. A neutral skills/fdw-shared/ was rejected: a top-level directory with no SKILL.md either fails to ship, if the installer enumerates skills by SKILL.md, or registers as a tenth skill if it enumerates directories. Living inside a real skill has neither failure mode.
